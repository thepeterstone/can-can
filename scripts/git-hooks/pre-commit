#!/bin/sh
# Blocks commits directly to 'master' — development happens on 'main'.
branch=$(git branch --show-current)
if [ "$branch" = "master" ]; then
  echo "Error: commits to 'master' are not allowed. Use 'main'."
  exit 1
fi
